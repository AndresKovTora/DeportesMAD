# Carpeta src
Esta carpeta es la que contiene el código fuente del proyecto. Estos son los ficheros `archivo.c` en donde estará escrito el código fuente (source, de ahí src, ¿has visto que listo?) de librerías personalizadas y el `main.c`.
Para información detallada sobre librerías, ver la [carpeta de headers](../include/README.md).
